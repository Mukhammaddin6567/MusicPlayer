package uz.gita.musicplayer.domain.repository.impl

import android.database.Cursor
import javax.inject.Inject

/*
class AppRepositoryImpl
@Inject constructor(
    private val musicDao: MusicDao
) : AppRepository {

    override suspend fun saveAllMusic(musicList: List<MusicEntity>) {
        musicDao.saveAllMusic(musicList)
    }

    override fun getAllMusicByQuery(query: String): Cursor = musicDao.getAllMusicByQuery(query)

    override fun getAllMusic(): Cursor = musicDao.getAllMusic()

}*/
