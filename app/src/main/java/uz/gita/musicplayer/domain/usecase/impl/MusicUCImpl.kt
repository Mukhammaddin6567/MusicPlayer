package uz.gita.musicplayer.domain.usecase.impl

/*
class MusicUCImpl
@Inject constructor(
    private val repository: AppRepository
) : MusicUC {

    override fun allMusic() = flow<Cursor> {
        delay(500)
        emit(repository.getAllMusic())
    }.flowOn(Dispatchers.IO)

    override fun searchByQuery(query: String) = flow<Cursor> {
        delay(500)
        emit(repository.getAllMusicByQuery("%query%"))
    }.flowOn(Dispatchers.IO)

}*/
